# 更新日志

本文档记录 **内网软件助手** 近期功能与行为变更（基于开发与对话整理）。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号待你方发布时自行标注。

## [未发布]

### 新增

- **共享目录自动目录树**：根据 `config.json` 中的 `share_root` 递归扫描，多级子文件夹与其中 `.exe` / `.msi` 自动映射为树形列表，无需维护独立软件清单文件。
- **目录与界面同步**：对共享根及子目录使用 `QFileSystemWatcher`；UNC 网络路径下增加定时轮询；提供「刷新目录」按钮。
- **下载前校验**：若本地下载目录已存在同名安装包，弹窗提示「可能已下载过」并确认是否覆盖下载。
- **下载完成后**：询问是否打开下载所在文件夹（资源管理器）。
- **安装按钮状态**：根据当前选中项对应的本地下载文件是否存在，自动启用或禁用「安装」。
- **`app/catalog.py`**：目录扫描、签名检测与 `CatalogDirNode` 数据结构。
- **`downloader.expected_local_download_path`**：与写入逻辑一致的本机目标路径计算。

### 变更

- **配置来源**：删除对 `software_list.json` 的依赖；软件列表完全由共享目录结构决定。
- **界面**：左右分栏、卡片式区域、树控件样式与按钮配色；树节点仅显示文件夹名/文件名（详情区用「位置」展示层级名称，不强调完整 UNC）。
- **安装流程**：移除「自动静默安装」与模式单选；**安装** 仅在本地已有对应文件时可点，点击后询问是否立即运行，使用系统默认方式启动安装包（等同双击，弹出安装向导）。
- **`SoftwareItem`**：移除 `silent_args`；展示用 `breadcrumb`（纯文件夹名层级）。

### 修复

- **下载模块**：HTTP 分支中 `urlparse` 误用未定义变量的问题（改为使用实际源地址字符串）。
- **树形列表**：在样式表中为 `QTreeWidget` 与条目显式设置前景色，避免文字显示为透明/白色。

### 移除

- **`software_list.json`** 及其加载逻辑。
- **静默安装相关**：`run_silent_installer`、`.args` 旁路参数文件读取。
- **资源管理器 `open_in_explorer`**（由按场景打开「下载所在文件夹」的 `open_download_folder` 替代）。

---

## 说明

- 早期若仓库中仍保留 `software_list.json` 示例，可手动删除；当前版本不再读取该文件。
- 发布新版本时，建议将 `[未发布]` 改为具体版本号与日期，并在此文件顶部追加新版本区块。
